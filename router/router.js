const Router = (() => {
    return {
        routes: [],
        mode: null,
        root: '/',

        config(options) {
        },

        getFragment() {
        },

        clearSlashes(path) {
        },

        add(regexp, handler) {
        },

        remove(param) {
        },

        flush() {
        },

        check(f) {
        },

        start() {
        },

        navigate(path) {
        }
    };
})();